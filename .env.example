# ===========================================
# PawLog Frontend - Environment Variables
# ===========================================
# Copy to .env and fill in your values.
# For local: use localhost URLs. For prod deploy: use your backend/frontend URLs.
# ===========================================

# Backend API URL (required for API calls)
# Local dev: leave empty to use Vite proxy (same-origin, avoids CORS/referrer issues)
# Local direct: http://localhost:8080
# Prod:   https://your-backend.onrender.com (or Railway, Fly.io, etc.)
# VITE_API_URL=

# Google OAuth Client ID (required for Sign in with Google)
# Get from: Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID
# Add Authorized JavaScript origins: http://localhost:5173 (local), https://your-frontend.vercel.app (prod)
VITE_GOOGLE_CLIENT_ID=526641416257-b7fh4gajd9quivctoctc6v3e4o9k4per.apps.googleusercontent.com

